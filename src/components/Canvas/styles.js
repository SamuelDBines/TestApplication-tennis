import styled from "styled-components";
import { Card, Button } from "react-materialize";

export const StyledCard = styled(Card)`
  background: #2a1843;
  width: 100%;
  height: 80vh;
  overflow-y: auto;
`;
export const StyledButton = styled(Button)`
  background: rgba(85, 49, 135, 0.47);
`;
